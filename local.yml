- hosts: localhost 
  become: true
  pre_tasks: 
    - name: Update Repositories
      dnf: update_cache=yes
      changed_when: False
  
  tasks:   
     - include: home/rayweatherford/ansible/ansible/tasks/packages.yml
